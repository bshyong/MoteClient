{
  "name": "MoteClient",
  "version": "1.0.0",
  "description": "A command line client to access the Mote server",
  "main": "MoteClient.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kent Chen",
  "bin": {
    "MoteClient": "MoteClient.js"
  },
  "dependencies": {
    "commander": "^2.6.0",
    "mqtt": "^1.1.0",
    "node-uuid": "^1.4.2"
  }
}
